# تشغيل الباك إند محلياً

## المتطلبات
- Python 3.8+

## خطوات التشغيل

### 1️⃣ تثبيت المكتبات
```bash
pip install -r Requirements.txt
```

### 2️⃣ تشغيل السيرفر
```bash
cd src
python app.py
```

السيرفر سيشتغل على: **http://127.0.0.1:5000/**

### 3️⃣ اختبار الاتصال
افتح المتصفح وروح:
```
http://127.0.0.1:5000/api/health
```

إذا شُفت: `{"status": "ok", "service": "munjiz-backend"}` ✅ السيرفر شغال

## الملفات المهمة
- `src/app.py` — السيرفر الرئيسي
- `src/routes/` — جميع الـ API endpoints
- `src/services/` — منطق العمل
- `data/munjiz.db` — قاعدة البيانات (تُنشأ تلقائياً أول مرة)
- `frontend/index.html` — الواجهة (هذي اختياري، الفرونت يمكن يكون منفصل)

## ملاحظات
- قاعدة البيانات تُنشأ تلقائياً من `src/seed/seed.py` أول ما السيرفر يشتغل إذا ما كانت موجودة
- السيرفر يفعّل CORS تلقائياً (يقبل طلبات من أي رابط)
- البوابات API موثقة في ملفات `src/routes/`
